# Copy to .env — docker compose reads this file for variable substitution.

# Port published on the host (mapped to 3000 inside the container).
PORT=3000

# Enables the ?admin=true admin view. Leave empty to disable it.
ADMIN_PASSWORD=
